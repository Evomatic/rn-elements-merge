import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="button"
    disabled={false}
    type="outline"
    loading
    title="Loading button">

  </Button>
);
